export enum MODE {
    window,
    browser,
    cloud,
    chrome
}

export enum OS {
    Linux,
    Windows,
    Darwin,
    FreeBSD,
    Unknown
}

export enum ARCH {
    x64,
    arm,
    itanium,
    ia32,
    unknown
}
